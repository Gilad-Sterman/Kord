import { VibeCard } from "./VibeCard"

export function TopVibes() {

    return (
        <section className="top-vibes" >
            <h4>Top Categories</h4>
            {/* <div className="vibe-container">
                {topVibes.map(vibe => <VibeCard key={vibe} vibe={vibe} />)}
            </div> */}
        </section>
    )
}